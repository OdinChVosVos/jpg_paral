# JPEG Parallel Encoder

Библиотека параллельного JPEG-сжатия с реализацией ключевых алгоритмов (DCT, квантование, цветовые преобразования) на C++ с использованием OpenMP. Включает Python биндинги и графический интерфейс для визуализации и тестирования.

## Возможности

- ✨ Полная реализация базового JPEG кодирования с нуля
- 🚀 Параллельная обработка с помощью OpenMP
- 🐍 Python API через pybind11
- 🎨 GUI для интерактивного тестирования
- 📊 Встроенные бенчмарки производительности
- 🔧 Настраиваемое качество и количество потоков



### Поток данных

1. **RGB → YCbCr** — конвертация цветового пространства (параллельная)
2. **Разбиение на блоки 8×8** — подготовка к DCT
3. **DCT преобразование** — преобразование в частотную область (оптимизированное с предвычисленными косинусами)
4. **Квантование** — сжатие с потерями (зависит от параметра качества)
5. **Упаковка данных** — формирование выходного потока

## Требования

### Обязательные зависимости

- **C++17** или выше (GCC 7+, Clang 5+, MSVC 2017+)
- **CMake** 3.15+
- **OpenMP** — для параллелизации
- **Python 3.8+** — для GUI и биндингов

### Опциональные зависимости

- **pybind11** — для Python биндингов (git submodule)
- **Tkinter** — для GUI (обычно встроен в Python)
- **Pillow** — для работы с изображениями в Python
- **NumPy** — для манипуляций с массивами

## Установка

### 1. Клонирование репозитория

```
git clone --recursive https://github.com/yourusername/jpeg-parallel.git
cd jpeg-parallel
```

> **Важно:** Используйте `--recursive` для автоматической загрузки submodule pybind11

Если забыли `--recursive`:

```
git submodule update --init --recursive
```

### 2. Установка зависимостей

**Ubuntu/Debian:**

```
sudo apt update
sudo apt install build-essential cmake libomp-dev python3-dev python3-tk python3-pip
pip3 install pillow numpy
```

**Fedora:**

```
sudo dnf install gcc-c++ cmake libomp-devel python3-devel python3-tkinter python3-pip
pip3 install pillow numpy
```

**Windows (WSL):**

Следуйте инструкциям для Ubuntu в WSL окружении.

### 3. Сборка проекта

```
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j$(nproc)
```

Успешная сборка создаст:
- `libjpeg_lib.a` — статическая библиотека C++
- `jpeg_test` — исполняемый файл для бенчмарков
- `jpeg_py*.so` — Python модуль

## Использование

### Запуск GUI

```


# Скопировать Python модуль в корень проекта

cd /path/to/jpeg-parallel
cp build/jpeg_py*.so .

# Запустить GUI

python3 gui.py

```

**Управление GUI:**
- **1, 2, 3** — переключение тестовых паттернов
- **↑/↓** — изменение качества (10-100)
- **←/→** — изменение количества потоков (1-16)
- **Кнопка "Сжать"** — выполнить кодирование

### Запуск бенчмарков

```

cd build
./jpeg_test

```

Вывод покажет:
- Влияние количества потоков на производительность
- Зависимость размера/времени от качества
- Производительность на разных разрешениях (512×512 до 4096×4096)

## Описание модулей

### CMakeLists.txt

Конфигурационный файл CMake для автоматизации сборки. Устанавливает стандарт C++17, флаги оптимизации (`-O3`, `-march=native`), подключает OpenMP и Python через pybind11. Включает опцию `CMAKE_POSITION_INDEPENDENT_CODE` для корректной линковки в shared library.

### include/dct.h и src/dct.cpp

**Дискретное косинусное преобразование** — ядро JPEG-сжатия. Класс `DCT` предоставляет:
- `forward_dct()` — стандартная реализация 2D DCT
- `forward_dct_optimized()` — оптимизированная версия с предвычисленными косинусами
- `inverse_dct()` — обратное преобразование

**Оптимизация:** Таблица `cosine_table` предвычисляется один раз при инициализации, сокращая время выполнения на ~40%.

### include/quantization.h и src/quantization.cpp

**Квантование** — этап с потерями, определяющий степень сжатия. Содержит:
- `LUMINANCE_QUANT_TABLE` — стандартная матрица для яркости
- `CHROMINANCE_QUANT_TABLE` — матрица для цветности
- `scale_quantization_table()` — масштабирование по качеству (1-100)
- `quantize()` / `dequantize()` — прямое/обратное квантование

**Формула масштабирования:**
- Качество < 50: `scale_factor = 5000 / quality`
- Качество ≥ 50: `scale_factor = 200 - quality * 2`

### include/jpeg_encoder.h и src/jpeg_encoder.cpp

**Главный класс-энкодер**, координирующий весь процесс:

**Ключевые методы:**
- `rgb_to_ycbcr()` — цветовое преобразование (параллельное через `#pragma omp parallel for`)
- `process_block()` — обработка блока 8×8: смещение на 128, DCT, квантование
- `process_channel_parallel()` — параллельная обработка канала (`#pragma omp parallel for schedule(dynamic)`)
- `encode()` — основная функция, использующая `#pragma omp parallel sections` для одновременной обработки Y, Cb, Cr каналов

**Измерение времени:** Использует `omp_get_wtime()` для точной оценки производительности.

### src/python_bindings.cpp

**Pybind11 биндинги** для экспорта C++ API в Python:
- Обертка классов `Image`, `EncodedData`, `JPEGEncoder`
- Автоматическая конвертация между Python и C++ типами
- Сохранение производительности (критичные операции в C++)

### test/test_encoder.cpp

**Модуль тестирования:**
- `generate_test_image()` — синтетические изображения (градиент, шахматы, волны)
- `benchmark_threads()` — производительность vs. потоки
- `benchmark_quality()` — размер/время vs. качество
- Автоматическое тестирование на разных разрешениях

### gui.py

**Python GUI** на Tkinter:
- Отображение изображений через PIL/ImageTk
- Интерактивные слайдеры для качества и потоков
- Генерация тестовых паттернов
- Отображение статистики сжатия в реальном времени


### Настройка качества

| Качество | Описание | Использование |
|----------|----------|---------------|
| 10-30 | Максимальное сжатие, низкое качество | Превью, миниатюры |
| 50-75 | Баланс размера и качества | Веб-изображения |
| 85-95 | Высокое качество, минимальное сжатие | Печать, архивы |


## Лицензии

Данный проект использует следующие библиотеки с открытым кодом:

### pybind11

Python биндинги реализованы с использованием pybind11.
- **Лицензия:** BSD-3-Clause
- **Copyright:** (c) 2016 Wenzel Jakob
- **Полный текст:** [LICENSE_PYBIND11](LICENSE_PYBIND11)
- **Репозиторий:** https://github.com/pybind/pybind11

## Ссылки

- [JPEG Standard (ITU-T T.81)](https://www.w3.org/Graphics/JPEG/itu-t81.pdf)
- [OpenMP Specification](https://www.openmp.org/specifications/)
- [pybind11 Documentation](https://pybind11.readthedocs.io/)